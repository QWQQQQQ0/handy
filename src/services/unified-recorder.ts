// Backward-compatible re-export barrel.
// The UnifiedRecorder implementation lives in services/recorder/.
// See services/recorder/index.ts for the full implementation.

export { unifiedRecorder } from './recorder/index';
